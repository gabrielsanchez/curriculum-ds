# Supervised vs. Unsupervised Learning

## Learning Objectives

- Differentiate supervised (regression, classification) from unsupervised (clustering, dimensionality reduction).
- Match problem types to the correct ML approach.
